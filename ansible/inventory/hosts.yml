---
# =============================================================================
# PROXMOX HOMELAB INVENTORY
# =============================================================================
# Define your Proxmox VE hosts here

all:
  children:
    proxmox:
      hosts:
        # Replace with your actual Proxmox host details
        pve1:
          ansible_host: 192.168.178.5
          ansible_user: root
      # pve2:
      #   ansible_host: 192.168.178.11
      #   ansible_user: root
      # pve3:
      #   ansible_host: 192.168.178.12
      #   ansible_user: root
      vars:
        # Common variables for all Proxmox hosts
        ansible_python_interpreter: /usr/bin/python3
        ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"

    # You can add other host groups here as your homelab grows
    # docker:
    #   hosts:
    #     docker1:
    #       ansible_host: 192.168.1.200
    # k8s:
    #   hosts:
    #     k8s-master:
    #       ansible_host: 192.168.1.210
